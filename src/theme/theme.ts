import { ThemeConfig } from "antd";

const lightTheme: ThemeConfig = {
  token: {
    // Seed Token
    colorPrimary: "#2ED66E",
    // Alias Token
    // colorBgBase: "#E7EDFF",
    colorBgLayout: "#E7EDFF",
  },
};

export { lightTheme };
