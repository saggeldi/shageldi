import { Box } from "@mui/material";
import React from "react";

const Footer = () => {
  return (
    <Box
      sx={{
        backgroundColor: "black",
        width: "100%",
        height: "60px",
        position: "fixed",
        bottom: "0",
      }}
    ></Box>
  );
};

export default Footer;
